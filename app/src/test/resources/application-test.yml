spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/test
  jpa:
    show-sql: true
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        hbm2ddl:
          auto: create
